# Config Importer Fixtures

These small, file-backed fixtures prove the pinned optional parser protocol:

- `terraform.tf` covers module qualification, managed-resource references,
  explicit dependencies, and exclusion of variable references.
- `schema.sql` covers schema-qualified PostgreSQL tables, primary keys, and a
  named foreign key.

They are command evidence, not large-corpus or complete-dialect evidence.
