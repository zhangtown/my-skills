# 超大二进制资源（>100MB，GitHub 单文件上限；换机时单独拷贝）
*.pyz
node_modules/
__pycache__/
*.pyc
.DS_Store
# Agent 本机标记文件
.disable_to_model_invocation_migration.json
Thumbs.db
*.tmp
.skills-manager.lock
# 本地模型权重（大资产，不入库，换机单独拷贝；技能代码仍入库）
qwen-tts__skillhub/model_local/
qwen-tts__skillhub/hf_cache/


# kimi-pptx 系列打包的 DSL 运行时（约 250MB，超 GitHub 100MB 单文件上限；本地使用，不入库，换机单独拷贝）
kimi-pptx/scripts/kimi_ppt_dsl.pyz
kimi-pptx-swarm/scripts/kimi_ppt_dsl.pyz

# ---- 手工维护：本地大资产，绝不入库（勿删；skills-manager 的 auto backup 曾清掉过）----
# 注意：不要忽略 /.skills-manager/ 本身 —— 那里的 protocol.json / skills/<uuid>.json 是 skills-manager
# 的合并元数据，已被 git 跟踪且每次 sync 都会提交，整目录忽略会挡掉将来新生成的元数据。
/qwen-tts/
/qwen-tts__skillhub/
*.safetensors
venv/

# skills-manager: oversized skills excluded from backup (auto-managed)
/qwen-tts/
/.skills-manager/skills/1af82b45-ba71-4556-9d69-44007f301bc0.json
# skills-manager: end oversized skills
